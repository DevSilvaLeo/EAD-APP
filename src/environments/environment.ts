export const environment = {
  production: false,
  /** URL base da API (sem barra final). Ex.: http://localhost:5000 */
  apiUrl: 'http://localhost:5000',
  /** Quando true, o AuthService usa respostas mockadas (sem HTTP). Desligue e configure apiUrl para integrar o backend. */
  useMock: true,
};
